# Infrastructure

Concrete implementations for filesystem, process, network, provider APIs, MCP,
local tools, and persistence.

Infrastructure may implement kernel and application ports. It must not depend on
apps or UI.
