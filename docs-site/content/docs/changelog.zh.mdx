---
title: Changelog
description: 从 SOBA Agent git 标签和提交主题生成的版本历史。
---

{/* 生成文件。不要手动编辑。 */}

此页面由仓库标签和提交主题生成。发布前运行 `bun run docs:changelog`，并在 CI 中运行 `bun run docs:changelog:check`。

## Unreleased

`v0.6.0` 之后的更改。发布时请更新版本并为 release commit 打 tag。

Local range: `v0.6.0..HEAD`

### Documentation

- make v0.6 documentation baseline
- add versioned documentation snapshots

## v0.6.0

2026-06-29 发布为 `v0.6.0`。

[Compare](https://github.com/avacadorun-dev/soba-agent/releases/tag/v0.6.0)

此范围内没有提交。
