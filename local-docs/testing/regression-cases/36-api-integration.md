# Регресс-кейсы: API и интеграция

## Цель
Проверить OpenResponses API clients, middleware-адаптеры, compliance-тесты.

## Окружение
- `.soba` собран
- Реальный API (OpenAI-compatible)
- Возможность запуска compliance-тестов

---

## Кейс 01: Built-in провайдер (DeepSeek, Kimi, Alibaba, OpenRouter)

**Шаги:**
1. `soba provider use deepseek`
2. `soba --model <модель-из-discovery> "Тест"`

**Ожидаемый результат:** Ответ от выбранной модели через DeepSeek.

**Критерий PASS:** Не пусто. Модель не хардкод — пользователь указывает сам.

---

## Кейс 02: Кастомный провайдер (soba provider add)

**Шаги:**
1. `soba provider add --id my-provider --name "My Provider" --base-url https://... --api-key-env MY_KEY --model my-model-7b`
2. `soba provider use my-provider`
3. `soba --model my-model-7b "Тест"`

**Ожидаемый результат:** Ответ от кастомного провайдера.

**Критерий PASS:** Не пусто.

---

## Кейс 03: Compliance-тесты (если есть)

**Шаги:**
1. `bun run validation/bin/compliance-test.ts`

**Ожидаемый результат:** Все тесты проходят.

**Критерий PASS:** 0 fails.

---

## Кейс 04: OpenAPI спецификация валидна

**Шаги:**
1. Проверить `validation/public/openapi/openapi.json`

**Ожидаемый результат:** Валидная OpenAPI 3.0 спецификация.

**Критерий PASS:** Нет ошибок валидации.

---

## Кейс 05: Zod-схемы генерируются

**Шаги:**
1. Проверить `validation/src/generated/kubb/zod/`

**Ожидаемый результат:** Zod-схемы всех типов существуют.

**Критерий PASS:** Файлы присутствуют.

---

## Кейс 06: Stream-test работает

**Шаги:**
1. `bun run validation/stream-test.ts`

**Ожидаемый результат:** Тест проходит.

**Критерий PASS:** 0 errors.

---

## Кейс 07: Middleware передаёт все поля

**Шаги:**
1. Проверить debug-логи на соответствие OpenResponses-формату

**Ожидаемый результат:** Все поля присутствуют (id, type, role, content, tool_calls).

**Критерий PASS:** Полнота.

---

## Кейс 08: Провайдер с streaming

**Шаги:**
1. `soba provider use deepseek`
2. `soba --model <модель> --stream "Расскажи о себе"`

**Ожидаемый результат:** Постепенный вывод.

**Критерий PASS:** Streaming работает.

---

## Кейс 09: Провайдер без streaming (fallback)

**Шаги:**
1. `soba provider use deepseek`
2. `soba --model <модель-без-streaming> "Тест"`

**Ожидаемый результат:** SOBA переключается на non-stream.

**Критерий PASS:** Response получен.

---

## Кейс 10: Rate limit handling

**Шаги:**
1. Отправить 50+ запросов за 1 минуту

**Ожидаемый результат:** SOBA обрабатывает 429 ошибки, ждёт и retry.

**Критерий PASS:** Все запросы (или их часть) выполнены.

---

## Кейс 11: API key placeholder в логах

**Шаги:**
1. `.soba --debug "Тест"`
2. Проверить debug-логи

**Ожидаемый результат:** API key не выводится в логах (заменён на "***").

**Критерий PASS:** Безопасность.

---

## Кейс 12: Long-running запрос (5+ минут)

**Шаги:**
1. Отправить запрос на генерацию большого кода

**Ожидаемый результат:** Ответ получен.

**Критерий PASS:** Не таймаут.

---

## Кейс 13: Invalid base-url (не URL)

**Шаги:**
1. `.soba --base-url "not-a-url" --api-key test --model test "hi"`

**Ожидаемый результат:** Ошибка валидации URL.

**Критерий PASS:** Сообщение.

---

## Кейс 14: Максимальный размер ответа (большой stream)

**Шаги:**
1. Запросить генерацию 10,000 строк

**Ожидаемый результат:** Ответ получен (возможно, обрезан).

**Критерий PASS:** Не падает.

---

## Кейс 15: Маппинг ошибок провайдера

**Шаги:**
1. Отправить невалидный запрос (например, превышение max_tokens)

**Ожидаемый результат:** OS error mapped to SOBA error.

**Критерий PASS:** Понятное сообщение.
