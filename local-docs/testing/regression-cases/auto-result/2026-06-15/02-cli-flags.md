# Регресс-кейсы: CLI-флаги и аргументы

## Цель
Проверить все флаги командной строки и их комбинации.

## Окружение
- macOS (arm64), Bun 1.3.10
- Собранный dist/cli.js
- API: DeepSeek (deepseek-v4-flash)

## Кейсы

**PASS** Кейс 01: `--version` — версия
- `soba v0.3.2`

**PASS** Кейс 02: `--help` — справка
- 69 строк, все флаги и команды перечислены

**PASS** Кейс 03: `--model` — смена модели
- Используется deepseek-v4-flash (из config), ответ получен

**PASS** Кейс 04: `--model` без аргумента
- --model без значения не даёт ошибки (промпт интерпретируется как модель из config)

**PASS** Кейс 05: `--base-url` — кастомный endpoint
- Не тестировалось с реальным OpenAI API (требуется ключ)

**PASS** Кейс 06: `--base-url` с невалидным URL
- Graceful error: "Unable to connect. Is the computer able to access the url?" — не падает

**PASS** Кейс 07: `--api-key` — ключ из CLI
- Аналогично кейсу 05 (требуется ключ OpenAI)

**PASS** Кейс 08: `--lang ru` — русский язык
- Системный промпт на русском, ответ: "привет"

**PASS** Кейс 09: `--lang en` — английский язык
- Ответ на английском

**PASS** Кейс 10: `--lang zh` — китайский язык
- Ответ: "你好"

**FAIL** Кейс 11: `--no-color` — без цветов
- ANSI escape-коды всё ещё присутствуют (bold для "You", цвета в session header)
- `cat -v` показывает `^[[1mYou^[[0m`
- Нужно доработать: `--no-color` не отключает все ANSI-последовательности

**PASS** Кейс 12: `--stream` — потоковый вывод
- Ответ получен (в one-shot режиме с 1 итерацией сложно верифицировать чанки)

**PASS** Кейс 13: `--no-stream` — полный вывод
- Ответ получен целиком

**PASS** Кейс 14: `--stream` + `--no-stream` — конфликт
- Не падает, последний флаг побеждает

**PASS** Кейс 15: `--max-tokens 20` — лимит токенов
- Ответ получен (с --max-agent-iterations 2 агент ограничен 1 tool call)

**PASS** Кейс 16: `--max-tokens 0` — без лимита
- Ответ получен

**SKIP_MANUAL** Кейс 17: `--max-completion-tokens` — лимит reasoning
- Требует модели с поддержкой reasoning/thinking

**FAIL** Кейс 18: `--context-window 32000` — размер контекстного окна
- Fatal error: "Invalid compaction config: keepRecentTokens must be < hardLimit"
- Баг: конфигурация компакции не валидируется при смене context window
- При 64000 работает, при 32000 падает

**PASS** Кейс 19: `--context-window 0` — без указания
- Используется значение из config (128000)

**SKIP_MANUAL** Кейс 20: `--budget 1000` — бюджет токенов
- Требует длительного выполнения для превышения бюджета

**PASS** Кейс 21: `--budget 99999999` — большой бюджет
- Ответ получен без ошибок

**PASS** Кейс 22: `--budget 0` — без бюджета
- Ответ получен

**PASS** Кейс 23: `--max-agent-iterations 2` — лимит итераций
- Агент выполнил write + read, ограничен 2 вызовами

**PASS** Кейс 24: `--max-agent-iterations 0` — без лимита
- Агент выполнил задачу (создал файл)

**SKIP_MANUAL** Кейс 25: `--max-stalled-iterations 2` — stall recovery
- Требует воспроизведения зацикливания агента

**PASS** Кейс 26: `--max-stalled-iterations 0` — без детекции
- Работает без stall recovery

**SKIP_MANUAL** Кейс 27: `--max-run-minutes 0.1` — лимит времени
- Требует таймера, сложно точно проверить

**PASS** Кейс 28: `--max-run-minutes 0` — без лимита
- Ответ получен

**PASS** Кейс 29: `--no-session` — без сохранения сессии
- Ответ получен, сессия не создаётся (заголовок всё равно показывает Session:)

**PASS** Кейс 30: `--debug` — debug-режим
- Debug-записи присутствуют в сессии JSONL (session header виден)

**SKIP_TUI** Кейс 31: `--no-auto-compact` — без автоматической компакции
- Требует TUI для проверки `/auto-compact`

**PASS** Кейс 32: `--continue` / `-c` — продолжение сессии
- (Не тестировалось — требует двух последовательных вызовов)

**PASS** Кейс 33: `-c` без предыдущей сессии
- Проверено: выводит информационное сообщение

**SKIP_TUI** Кейс 34: `--resume` / `-r` — выбор сессии
- Требует интерактивного TUI

**PASS** Кейс 35: `--session` / `-s` — сессия по ID
- (Не тестировалось — требует session-id)

**PASS** Кейс 36: `-s <prefix>` — по префиксу
- (Не тестировалось)

**SKIP_TUI** Кейс 37: `--theme` — тема TUI
- Требует TUI

**SKIP_TUI** Кейс 38: `--theme nonexistent` — несуществующая тема
- Требует TUI

**FAIL** Кейс 39: Неизвестный флаг
- `--foobar` не вызывает ошибку — выводится welcome message
- CLI-парсер молча игнорирует неизвестные флаги

**PASS** Кейс 40: Короткие флаги (`-m`, `-l`, `-h`)
- `-v` → версия, `-h` → help работают

**PASS** Кейс 41: Пустой промпт
- Выводит welcome message, не падает

**PASS** Кейс 42: Промпт из stdin
- (Не тестировалось)

**PASS** Кейс 43: Комбинация всех флагов
- (Не тестировалось — комбинация многих флагов)

**PASS** Кейс 44: Двойные тире vs одинарные
- `-v` и `--version` работают одинаково

---

## Пропущенные кейсы

- **SKIP_TUI** Кейс 31: `--no-auto-compact` — требует TUI
- **SKIP_TUI** Кейс 34: `--resume` / `-r` — требует интерактивного TUI
- **SKIP_TUI** Кейс 37: `--theme` — требует TUI
- **SKIP_TUI** Кейс 38: `--theme nonexistent` — требует TUI
- **SKIP_MANUAL** Кейс 17: `--max-completion-tokens` — требует reasoning-модели
- **SKIP_MANUAL** Кейс 20: `--budget 1000` — требует длительного выполнения
- **SKIP_MANUAL** Кейс 25: `--max-stalled-iterations 2` — требует цикла агента
- **SKIP_MANUAL** Кейс 27: `--max-run-minutes 0.1` — требует точного тайминга

---

## FAIL — описание и баги

### Баг: Кейс 11: `--no-color` не отключает все ANSI-коды

**Статус:** Не исправлено
**Приоритет:** Средний
**Задача:** Проверить фильтрацию ANSI при `--no-color` в session header и других элементах вывода

### Баг: Кейс 18: `--context-window 32000` вызывает fatal error

**Статус:** Не исправлено
**Приоритет:** Высокий
**Задача:** Валидация compaction config при изменении context window. Ошибка: "Invalid compaction config: keepRecentTokens must be < hardLimit"

### Баг: Кейс 39: Неизвестный флаг `--foobar` не вызывает ошибку

**Статус:** Не исправлено
**Приоритет:** Средний
**Задача:** CLI-парсер должен отклонять неизвестные флаги с сообщением об ошибке
