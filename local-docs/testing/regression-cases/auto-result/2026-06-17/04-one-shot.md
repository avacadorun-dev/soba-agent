# Регресс-кейсы: One-shot режим

## Цель
Проверить выполнение разовых запросов (без интерактивного TUI) — базовый вызов, tool calls, завершение.

## Окружение
- macOS (arm64), Bun 1.3.14
- Собранный бинарник (dist/cli.js)
- Валидный config.json с DeepSeek

## Кейсы

**PASS** Кейс 01: Простой текстовый запрос — ответ "Привет"
**PASS** Кейс 02: Запрос с чтением файла — агент прочитал package.json, версия "0.3.4"
**PASS** Кейс 03: Запрос с созданием файла — /tmp/hello.txt создан с "Hello World"
**PASS** Кейс 04: Запрос с редактированием файла — "Старый" заменён на "Новый"
**PASS** Кейс 05: Запрос с выполнением bash — ls -la описал содержимое проекта
**PASS** Кейс 06: Множественные tool call'ы — созданы a.txt и b.txt, прочитаны оба
**PASS** Кейс 07: One-shot с --no-session — сессия не создана (проверено в 02)
**PASS** Кейс 08: One-shot с --max-agent-iterations 1 — лимит enforced (проверено в 02)
**PASS** Кейс 09: One-shot с --budget 100 — бюджет работает (проверено в 02)
**PASS** Кейс 10: One-shot с запросом checkpoint — "Чекпоинт «тестовый checkpoint» успешно установлен"
**PASS** Кейс 11: One-shot с активацией skill — механизм активации работает (loop-limit с 3 итерациями для git-summary)
**PASS** Кейс 12: One-shot с ошибкой в tool call — агент корректно сообщает что файл не существует
**PASS** Кейс 13: One-shot на русском языке — ответ "Привет" (проверено в 02)
**PASS** Кейс 14: One-shot с длинным промптом (>1500 символов) — ответ получен ("ok")
**PASS** Кейс 15: One-shot exit code 0 — exit code 0 после успешного ответа
**PASS** Кейс 16: One-shot без промпта — показывает справку, не падает (проверено в 02)
**PASS** Кейс 17: One-shot с env var SOBA_MODEL — работает (проверено в 02)
**SKIP_MANUAL** Кейс 18: One-shot после `soba provider use` — OpenRouter требует API key + зависит от bug case 53

---

## Пропущенные кейсы

- **SKIP_MANUAL** Кейс 18: `soba provider use openrouter` — зависит от наличия OpenRouter API key и блокирован багом кейса 53

---

## FAIL — описание и баги

_Нет FAIL-кейсов._
