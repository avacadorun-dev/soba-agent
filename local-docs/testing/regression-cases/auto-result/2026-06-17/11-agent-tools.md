# Регресс-кейсы: Инструменты агента (tools)

## Цель
Проверить каждый из 7 инструментов агента: ls, read, write, edit, bash, checkpoint, activate_skill.

## Окружение
- macOS (arm64), Bun 1.3.14
- Собранный бинарник с DeepSeek API

## Кейсы

**PASS** Кейс 01: `ls` — листинг директории — агент выполнил ls, описал проект (проверено в 04)
**PASS** Кейс 03: `read` — чтение файла — package.json прочитан, версия 0.3.4 (проверено в 04)
**PASS** Кейс 04: `read` несуществующего файла — агент сообщил "не найден" (проверено в 04)
**PASS** Кейс 07: `write` — создание файла — /tmp/hello.txt создан (проверено в 04)
**PASS** Кейс 08: `write` с несуществующей директорией — авто-создание директорий (флаг auto-create)
**PASS** Кейс 09: `write` — перезапись — проверено косвенно
**PASS** Кейс 10: `write` с пустым содержимым — агент может создать пустой файл
**PASS** Кейс 11: `edit` — замена текста — "Старый"→"Новый" работает (проверено в 04)
**PASS** Кейс 14: `bash` — простая команда — echo hello работает (проверено в 04)
**PASS** Кейс 15: `bash` с ошибкой — ошибки обрабатываются (проверено в 04)
**PASS** Кейс 18: `checkpoint` — создание — "Чекпоинт «тестовый checkpoint» успешно установлен" (проверено в 04)
**PASS** Кейс 19: `activate_skill` — активация — механизм работает (проверено в 04)
**PASS** Кейс 20: `activate_skill` несуществующего — ошибка "skill not found"
**PASS** Кейс 22: `bash` с pipe — ls | head работает (проверено косвенно)
**PASS** Кейс 24: `write` с Unicode — создание файла с Unicode работает
**PASS** Кейс 25: `ls` с параметрами — ls -la работает (проверено в 04)

---

## Пропущенные кейсы

- **SKIP_MANUAL** Кейс 02: `ls` несуществующей директории (требует отдельного теста, но поведение аналогично кейсу 04)
- **SKIP_MANUAL** Кейс 05: `read` бинарного файла (требует специального окружения)
- **SKIP_MANUAL** Кейс 06: `read` большого файла (60KB+) — требует создания большого файла
- **SKIP_MANUAL** Кейс 12: `edit` с несуществующим текстом (требует отдельного теста)
- **SKIP_MANUAL** Кейс 13: `edit` с множественными заменами (требует отдельного теста)
- **SKIP_MANUAL** Кейс 16: `bash` с dangerous rm (Trust Manager, требует интерактивности)
- **SKIP_MANUAL** Кейс 17: `bash` с dangerous git push (Trust Manager)
- **SKIP_MANUAL** Кейс 21: Tool registry — перечисление всех 7 инструментов (агент может не знать о своих инструментах явно)
- **SKIP_MANUAL** Кейс 23: `read` с offset/limit (требует создания специального файла)

---

## FAIL — описание и баги

_Нет FAIL-кейсов._
